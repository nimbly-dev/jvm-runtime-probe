# Spring Fixture Apps

This directory is reserved for real Spring applications used as integration-test fixtures.

Each fixture app should be self-contained and runnable with the built Java agent attached.

Planned first fixture:
- `catalog-app`
